{
    "httpMethod": "$context.httpMethod",
    "path": "$context.resourcePath",
    "query": {
        "type": "$input.params('type')",
        "limit": "$input.params('limit')",
        "lastKey": "$input.params('lastKey')"
    }
}
