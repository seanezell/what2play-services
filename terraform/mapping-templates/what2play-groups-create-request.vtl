{
    "http_method": "$context.httpMethod",
    "user_id": "$context.authorizer.claims.sub",
    "path": "$context.resourcePath",
    #set($body = $util.parseJson($input.body))
    "group_name": "$body.group_name",
    "member_ids": $input.json('$.member_ids')
}